name: road
resolution: 36
cells:
  intersection:
    asset: "intersection.png"
    left: [horizontal, top-right, bottom-right, intersection]
    right: [horizontal, top-left, bottom-left, intersection]
    top: [vertical, bottom-left, bottom-right, intersection]
    bottom: [vertical, top-left, top-right, intersection]
  horizontal:
    asset: "horizontal.png"
    left: [intersection, horizontal, top-right, bottom-right]
    right: [intersection, horizontal, top-left, bottom-left]
    top: [concrete, top-left, top-right, horizontal]
    bottom: [concrete, bottom-left, bottom-right, horizontal]
  vertical:
    asset: "vertical.png"
    left: [concrete, top-left, bottom-left, vertical]
    right: [concrete, top-right, bottom-right, vertical]
    top: [intersection, vertical, bottom-left, bottom-right]
    bottom: [intersection, vertical, top-left, top-right]
  bottom-left:
    asset: "bottom-left.png"
    left: [horizontal, bottom-right, top-right, intersection]
    right: [concrete, vertical, top-right, bottom-right]
    top: [concrete, horizontal, top-left, top-right]
    bottom: [vertical, top-right, top-left, intersection]
  bottom-right:
    asset: "bottom-right.png"
    left: [concrete, vertical, top-left, bottom-left]
    right: [horizontal, bottom-left, top-left, intersection]
    top: [concrete, horizontal, top-right, top-left]
    bottom: [vertical, top-right, top-left, intersection]
  top-left:
    asset: "top-left.png"
    left: [horizontal, bottom-right, top-right, intersection]
    right: [concrete, vertical, top-right, bottom-right]
    top: [vertical, bottom-right, bottom-left, intersection]
    bottom: [concrete, horizontal, bottom-left, bottom-right]
  top-right:
    asset: "top-right.png"
    left: [concrete, vertical, top-left, bottom-left]
    right: [horizontal, bottom-left, top-left, intersection]
    top: [vertical, bottom-right, bottom-left, intersection]
    bottom: [concrete, horizontal, bottom-right, bottom-left]
  concrete:
    asset: "concrete.png"
    left: [concrete, vertical, bottom-left, top-left]
    right: [concrete, vertical, bottom-right, top-right]
    top: [concrete, horizontal, top-left, top-right]
    bottom: [concrete, horizontal, bottom-left, bottom-right]
